apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openluffy-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/lebrick07/openluffy.git
    targetRevision: develop
    path: helm/openluffy
    helm:
      valueFiles:
        - values/dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: openluffy-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
